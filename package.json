{
  "name": "naomi-portfolio",
  "version": "0.0.1",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "clean-build": "trash _site && npx @11ty/eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "trash-cli": "^5.0.0"
  },
  "dependencies": {
    "terser": "^5.31.0"
  }
}
